{
  "name": "mubble",
  "description": "Library with projects",
  "version": "0.0.0",
  "scripts": {
    "build": "node mubble-cli.js build",
    "build:server": "echo 'building server \n\n' && cd packages/server && npm install && tsc && echo '\n\n server built\n\n'  && cd ../../ && pwd",
    "build:core": "cd packages/core && npm install && tsc && cd ../../",
    "build:browser": "cd packages/browser && npm install && npm run package && cd ../../"
  },
  "devDependencies": {
    "colors": "^1.3.0",
    "husky": "^4.3.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "node mubble-cli.js test"
    }
  }
}
